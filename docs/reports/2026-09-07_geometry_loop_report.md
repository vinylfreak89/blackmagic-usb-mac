# Report for the owner — geometry loop run unassisted (started 2026-09-07 04:30 JST; owner away)

**Status (2026-09-07 10:55 JST, loop paused for this report):** the engine's per-unit record at geometry-first-engine 8d0fdeb (run E) and Codex's reference at geometry-first-harness c4beaaa agree on the picture top in 607/608 + 608/608 (SP), 597/621 + 617/621 (EP), 469+18/607 + 604/607 (V-stabilize-off pass) and 581/604 + 499/607 (commercial, measurable both) units; Codex's harness does NOT accept the engine's record on any capture (its verdict, turn 12), for reasons that are all listed in §5 with the rows: the head-switch row S sits one row early where the partial line carries the other head's blanking (commercial 86 units, SP 3 of 12 exposed), a lone sub-black first line reads as the black line 22 (commercial field 2, 115 units), the raw pass's flagged first lines (120 units), and six EP units not yet read. The commercial tape's picture top is line 23/286 and its head switch moves between 260 and 261 on a stable picture (both instruments, Codex's four change counters 6645/6688/6714/6738); the owner's 'perfectly stable' holds for the picture, not for the switch row. Not stuck; the next iteration is defined (§5 items 1 and 9). Renders and machine read-backs of every frame are in §4b; nothing in this report is a judgement, only counts and rows.

## 1. What was measured (raw rows, both SP passes, commercial, EP)
- the switch signatures per row and the field's own variance (docs/geometry_first_engine.md §10, measured row signatures)
- the reliable count top..switch−1: 237 in both passes at units 20/105/200 (field 1)

## 2. The two instruments
- Codex's per-unit record (geometry-first-harness, commits e6a5542 + 35e5979): picture_top_line, expected_bottom = top+239, switch_first_line, bottom = last reliable = switch−1, hs_bottom = last picture-bearing band row, band_length incl. censored, status observed/inferred/censored/unmeasurable, dp/ds; 30/30 tests. SP census: f1 top 23×122/24×438/25×48, switch 259×7/260×119/261×433/262×49; f2 top 286×608, switch 522×511/523×92/524×5. Its RF status is `inferred` (the before-x gate withdrawn, agreed). Commercial observed subset constant at 23/259 and 286/521 from 551 on; 550/551 unmeasurable alike (the 551 rule lives in the test).
- Claude's engine measurement (experiments/switch_geometry.py, geometry-first-engine f326d31): top = first run of three picture rows above the field's own pedestal (CEA-608 rows excluded); S = first row entirely the other head (band scanned up from the clip; whole-row lag/ratio outside the picture envelope, or torn beyond the body's maxima, or pedestal, or intruded blanking); partial evidence for S−1; picture bottom = top+239 clipped. SP on pass (before the last top fix): S 261×459/260×73/262×67/259×5 (f1), 523×305/522×294/524×9 (f2); S changes unit to unit 168 (f1) / 250 (f2) = the switch jitter plus the partial ambiguity
- SP on pass, unit by unit (experiments/compare_records.py, engine 3d3103a vs Codex 82f177b): top agrees 608/608 in both fields (the four field-2 tops at 287 adopted by both); S vs switch_first_line within the one-row partial ambiguity in 608/608 (field 1: 553 exact / 51 / 4) and 607/608 (field 2: 389 / 209 / 9; unit 606: engine 520 vs reference 522 — the rows (zoom_SP_f2_u605-606.png) show 517–521 picture at luma 83–86, 522 the partial line with its right third departing, 523–525 pedestal: the reference is right (switch in 522, S 523) and the engine over-extended the band by two rows through its row-two-above test on that unit's diagonal content; recorded as an engine defect, not tuned away).
- commercial: both instruments had the dark-top defect in turn (Codex's v3 top 28 in 125 units, fixed on the rows in its turn 8 to 23; the engine's top 28/29 in 143 stable units with the same cause, fixed at 632b110: a dark recorded band is picture, a lone dark row before a bright one is the tape's black line 22, and the top lies within the first four recorded rows or is unmeasurable). Full-run agreement numbers follow the relaunch (runs at 632b110 in progress).
- V-stabilize-off pass (engine --repair vs Codex, slots matched): top agrees 591/607 (recorded field 1) and 605/607 (field 2); the engine's band detection on the raw pass is partial — the picture itself carries rows as torn as the band (a horizontal tear at lines 323–336 of unit 20), so the band is found only by its contiguity with the clip and by the other head's missing H-dip / whole-row time shift; recorded field 1 S at the verified units 524/523/523/523/523 against the rows' 523.

## 3. Blind checks (subagents that saw none of our code, own detectors, raw rows)
- field-2 switch moves at 17 units where Codex's reference changed the bottom (SP on pass): the switch row moved in 13, stayed in 4 (48, 50, 65, 81); over units 0..105 the switch sits on line 522 in 69 units, 523 in 35, 524 in 2, with the switch sample inside the row at 67..568 (the horizontal drift and wrap)
- engine S (first row belonging entirely to the other head) on 15 sampled SP units, both fields, after the final rule (commit above): field 1 agrees in 13/15 with 2 definitional (480: the partial line at the clip, the agent reports no full other-head row; 590: the agent calls the partial in-phase-part and names the pedestal), field 2 15/15. The agent's brief carried a typed S list instead of the engine's file values; its own measured S per unit is what these counts compare against.
- verified units (raw rows looked at by Claude, both passes): engine S = 261/259/262/261/261 (field 1) and 523/523/522/522/523 (field 2) at units 20/78/105/106/200, matching the rows (the switch is inside S or S-1; S-1's evidence is recorded, never resolved by a threshold)

- Codex turn 9 (17ea6df): the comb at the engine's residual units (see §5); EP pass: top 25×263/26×358 (f1), 288×621 (f2); switch 259×9/260×260/261×352 and 522×451/523×170; band 5–7 rows (the EP recording's picture ends further above the clip); comb 458 measurable, all shift 0, geometry disagreements none; fixed-top switch transitions 2 + 137 measurable, 0 changed the registration.
- Codex turn 8 (82f177b, raw-row audit): (1) commercial units 635–759: the top is line 23 (L23–L27 chroma-confirmed recorded rows, luma 4–10, no VBI waveform; L28 only the brighter onset) — its builder now applies the dark-band ruling; the comb +3 there collapses to 0 (104 decisive, 21 unmeasurable). (2) V-stabilize-off pass: with the correct adjacent partner (slot 2 of u with slot 1 of u+1) the expected comb baseline is +1 (reversed raster parity), and the 56 questioned units score 0 = a genuine one-line inter-field misregistration in the raw pass, which the capture cannot apportion between recording and playback. (3) SP unit 103: a genuine isolated +1 with both tops and switches fixed. (4) SP field-2 tops 287 at 87/258/439/467 adopted. 39 tests.

## 2b. Engine changes after the 632b110 records (2026-09-07 07:15–08:30 JST; each a commit on geometry-first-engine, each from rows looked at first)
- compare_records.py joined the engine's exact-unit index to the reference's counter-based ordinal; on the commercial tape they differ by 213 units, so the first commercial comparison was misaligned. Now joined by device counter (all four captures).
- 805434d: the pedestal = the flat run contiguous with the clip; a pedestal row must sit at that level. Rows: EP unit 3, lines 253–260 flat at luma 26 (std 1–2) above the band's pedestal 13 were taken as the other head's black and S ran up to 250.
- 770323e: a recorded row = chroma noise > 2x the blank's OR luma above the blank; padding excluded explicitly. Rows: commercial counter 6842, a flat grey field (luma 17–20, std 1.3, chroma noise 1.7x) read 'no-picture'.
- 6d6924f: the other head's horizontal blanking INSIDE the row (luma 1–2 at the blank's noise, samples ~60–200) is a band signature; length 64..200 samples (sync pulse .. H blanking, NTSC constants). Census against the reference (scratch blank_run_census.py): commercial band rows 834/954 (f1) and 1259/1323 (f2) carry one, the row before the switch 0/531 and 0/535, picture rows 0.4% and never within 4 rows of the switch; SP/EP band rows carry none (their intruding run sits at the pedestal). Rows: commercial counter 6672, S 262 → 260. Codex reached the same signature independently in turn 10 ('internal blank run x82–145 at Y1.391').
- 37a79cf: VBI-type rows by their lack of correlation with the row below (r < 0.5, a fitted default; measured VBI 0.01–0.36, picture 0.87–0.99), applied only to rows with texture ≥ 4x the field's own noise (median std of adjacent-row differences / √2 over the middle rows) so a dark band (commercial, std 3–6 vs noise 2) is left to the brightness rules. Rows: EP field 2 lines 286 (smeared bar) and 287 (run-in fragments) were the engine's top in 621/621 units; the top is 288.
- 6fdef82: S_tests / band_tests columns (which test carried each band row) — instrumentation.
- 0fc3461: streaming (two rasters in memory, never the capture) after four parallel runs holding 460–700 MB each pushed the host (17 GB, ~12 GB used by the owner's applications) into 5.5 GB of swap and a ~10x slowdown that looked like a hang; and an isolated flat bright row before a textured row is VBI (Codex's turn-10 witnesses, counters 2066/2303/2410: line 25 at 18.1/2.7 before line 26 at 107/65), a flat bright row before a flat row is picture (the flat grey field).
- experiments/stable_interval_check.py: the owner's commercial-tape rule as a test over an engine record (counter ≥ 6593: top constant, S within the one-row partial ambiguity of its mode; exit 1 on violations). Baseline at 632b110: 59 violations (top 4, S 55).

## 3b. Codex turn 10 (2390a2f, rows adjudication; 43 tests) — verdicts on the three disagreements
- EP field 1 top: the old reference was one line LATE (rule 'caption + 2' hard-coded); corrected at 426 of 621 units, 195 VBI-type rows retained. Of the engine's 601 one-line disagreements, 423 now agree; 178 remain where the engine's top sat on an isolated low-structure row before a structured one (fixed in 0fc3461, to be measured). Corrected histogram L24:207, L25:275, L26:139.
- EP field 2 top: the reference's 288 stands in 621/621; the engine's 286 was two lines early (fixed in 37a79cf). Capture-wide correlation medians: 286→287 0.10, 287→288 0.08, 288→289 0.93.
- Commercial switch: the engine's L260/L261 (first row entirely the other head) match the raw signature; the reference's switch_first_line names the first UNRELIABLE row (a measured partial predecessor), so a one-row difference is semantics, not error; two old reference errors (counters 6665, 6903: a weak 'partial' heuristic advanced the boundary) fixed. Stable interval, first-full-other-head row: L260 ×67, L261 ×464, unmeasurable 51; it CHANGES at counters 6645, 6688, 6714, 6738 with the top fixed at 23. Codex's objection, accepted: the owner's 'perfectly stable' holds for the picture top, not for the switch row or band length (they move by one row); the test allows S ± 1.
- Flat grey passage (counters 6805–6875): the reference now measures it (top 23/286, switch 260/261 and 522/523 observed) — only counters 6818 (field 1) and 6820 remain unmeasurable.

## 2e. Run C scored on all four captures (engine 0fc3461 vs references 2390a2f), and the changes it forced (992bf15)
- SP: top 608/608 both fields (the rebuilt reference reads 24 at unit 0, 25 at 105, 24 at 333/606; the engine the same); S within the partial ambiguity 607/608 (f1; unit 333 261 vs 259) and 607/608 (f2; unit 606 520 vs 522).
- V-stabilize-off pass: recorded field 1 (the SP's field 2) top agreed 482/550 measurable — a REGRESSION from 591/607: the new correlation test at zero lag excluded the raw pass's torn first lines (287–289 in 68 units, unmeasurable 57). Field 2 605/607. Fixed by the best-lag correlation (992bf15) for tears up to 24 samples; the flagged first lines beyond that remain a residual (units 61, 74: line 286 at 38/56 correlates 0.44 at best, top read 288).
- EP: field 1 top agreed 357, engine lower by 1–2 in 162, higher by 1 in 51, unmeasurable 51; field 2 509 agreed, +1 in 32, unmeasurable 80. Rows: the zero-lag correlation excluded picture rows at horizontal edges (counter 2303 line 26: 0.00 below, 0.94 above) and the noise estimate from adjacent ROWS (contaminated by vertical detail: 6 on the EP, 12–15 on the SP) let a textured line count as flat (counter 2066: line 26 at std 22 against a gate of 23). Both fixed in 992bf15 (two-sided best-lag correlation; noise from adjacent samples). S: −10..−14 in ~40 units = the whole-row-lag test on slanted picture content (counter 1967 lines 253–258, lag 3–5 at ratio 0.81–0.89): fixed by the time-base-step criterion (lag two rows up equals lag one row up).
- Commercial: see 2d; the top's dependence on an unknown pedestal is replaced by the sub-black rule (992bf15): counters 6672, 6842, 6907 read 23/286 in spot runs; 6645 stays 25 (residual).
- Run D (992bf15) launched 09:11 JST on all four captures; its numbers replace these in §2f.

## 3c. Codex turn 11 (a2767c9/810d1e5; scorer + 49 tests): score of run C (0fc3461)
- SP accepted with three exceptions (counters 13825, 13833: an exposed other-head row at 260 with engine S 261; 14106: engine S 520 where the departure begins at 522). Top 608/608 both fields; comb contradictions 0/591.
- V-stabilize-off, EP, commercial: not accepted at 0fc3461 — the same defects Claude's rows found and 992bf15 addresses (raw-pass tops: 57 false no-picture; EP: 51 no-picture and 162 tops one to two rows low; S inside the body on slanted content; commercial top chatter 25×51 and S chatter). Codex adds: on the raw pass the engine's move of field 2's top from 286 to 287 in 68 units is RIGHT (line 286 at the noise floor, 287 matching the SP picture row at r≈0.99) — its old reference was wrong there; and its two repaired-field-2 moves at counters 395/397 are wrong (line 23 noise-floor black, 24 the picture) — the sub-black rule's case. Comb contradictions: off 64/518, EP 113/249, commercial 2/331.
- Commercial stable interval, Codex's direct signature: first-full-other-head 260×67 / 261×464, changes at 6645, 6688, 6714, 6738; the engine matched two and moved one counter early at the other two.
- Objection (accepted, turn 12): the reference stores the first UNRELIABLE row, not the first-full-other-head row; exact S comparison needs a new column, requested.

## 2f. Run D (engine 992bf15) scored against the references at 2390a2f (joined by counter)
- SP: top 607/608 (f1; one unit −1) and 608/608 (f2); S within the partial ambiguity 606/608 (f1: unit 333 at 261 vs 259) and 607/608 (f2: unit 606 at 520 vs 522).
- V-stabilize-off pass: recorded field 2 (the SP's field 1) top 604/607 (+1 ×3; Codex's turn-11 units 395/397 now agree); recorded field 1 (the SP's field 2) top 469 agree, +1 ×18 (Codex's rows: line 286 at the noise floor, 287 the picture — the engine is right and the reference wrong there), +2 ×56 and +3 ×64 = the flagged first lines: with V-stabilize off the first one or two lines of the field carry a horizontal timing error that varies along the row (flagging), so no single lag aligns them with the row below and the correlation test reads them as VBI. A residual: the raw pass is the fixture for what the line TBC does, not a delivery target; a per-segment alignment would be the fix, not tried today. S: within one row in 519/577 (f1), 582/597 (f2), the rest ±2–4 on the raw pass's torn band.
- EP: field 1 top 597/621 (−1 ×18, +1 ×5, +2 ×1; from 357 at run C), field 2 617/621; S within one row in 618/621 (f1) and 617/621 (f2). The three field-1 exceptions (units 31, 141, 438) and the three field-2 ones (144, 195, 574) are not yet looked at on the rows (residual list).
- Commercial: S within one row of its mode in 548/549 (f1) and 559/559 (f2) measurable stable units (run C: 18+7 beyond) — the band tests on their physical premise closed the over-extension; the TOP regressed (23 ×425, 25 ×107, 26 ×10, 24 ×7; field 2 286 ×416, 287 ×109, 289 ×26, 288 ×11; unmeasurable 33/20): on this tape's flat and dark scenes the picture rows are noise-only (body rows correlate at 0.1–0.3), so the texture and correlation rules describe noise; and the sub-black band rule read a dark band's last row as VBI. Fixed as (m) the rules stand down when the field's own body rows do not correlate (their premise) and (n) a sub-black row inside or ending a sub-black band is picture — run E.

## 2g. Changes (m)/(n) after run D (8d0fdeb) and the finding behind them
- The commercial tape HAS setup: its pedestal (flat rows contiguous with the clip) is 9–11 like fixture A's 11.4, so its dark first lines at 3–5 are sub-black — crushed blacks of the picture (line 23 carries grey picture at 6842; the picture is stable). The per-unit sub-black rule reads a LONE sub-black first line as the tape's black line 22 (fixture A's displaced case, on which the SP's 608/608 depends) and the top one row low there: commercial counters 6645 (field 1) and 6672 (field 2) and their like. A per-source lock (the black line 22 is always sub-black, flat, at one row, never grey) would separate the tapes; not built today — the engine is a per-unit instrument by this round's design.
- (m) the correlation/texture rules apply only where the field's middle rows correlate at ≥ 0.5; (n) a sub-black band's last row is picture. Spot runs: SP 24/25/24; EP 25/24/26/25 + 288; commercial 6593–6599, 6672, 6842, 6907 at 23/286. Run E launched 09:36 JST.

## 2h. Run E (engine 8d0fdeb, the record handed to Codex's turn 12) — agreement with the references at 2390a2f, joined by counter
| capture | field | top agree / differ (engine − ref) | S within ±1 of switch_first_line | beyond |
|---|---|---|---|---|
| SP | 1 | 607 / −1 ×1 | 605 of 606 with S | unit 333 (261 vs 259) |
| SP | 2 | 608 / 0 | 607 of 608 | unit 606 (520 vs 522) |
| off pass (SP field 2 recorded) | 1 | 469 / +1 ×18 (engine right, Codex turn 11), +2 ×56, +3 ×64 (flagged first lines, residual) | 561 of 577 | 16 units ±2–3 |
| off pass (SP field 1 recorded) | 2 | 604 / +1 ×3 | 582 of 597 | 15 units ±2–4 |
| EP | 1 | 597 / −1 ×18, +1 ×5, +2 ×1 | 618 of 621 | units 31, 141, 438 |
| EP | 2 | 617 / −1 ×3, unmeasurable 1 | 617 of 620 | units 144, 195, 574 |
| commercial (measurable both) | 1 | 576 / −2 ×19 (engine 23 vs ref 25 'inferred'), −1 ×4, +1..+3 ×4 | 556 of 561 | rewind passage only |
| commercial | 2 | 499 / +1 ×98 (engine 287, the lone sub-black class), +2 ×1, +3 ×9 | 554 of 558 | rewind passage only |
Commercial stable interval (counter ≥ 6593, 582 units), the owner's rule as a test: field 1 measurable 582/582, top 23 ×548 / 24 ×24 / 25 ×9 / 26 ×1, S 261 ×443 / 260 ×136 / 262 ×2 / 259 ×1 (|S − mode| > 1 in 1 unit), bottom 262 in 582; field 2 measurable 579/582, top 286 ×455 / 287 ×115 / 289 ×9, S 523 ×542 / 524 ×19 / 525 ×10 / 522 ×5 (|S − mode| > 1 in 10, all the first units 6594–6603 at 525), bottom 525 in 579. Violations 169 (run C 131, run D 271, baseline 632b110 59 with 124 unmeasurable): the top's remaining chatter is the lone sub-black first line (§2g), field 2 above all.

## 4b. Renders from run E (1670×486 x264, four fields per frame: two captures × two fields, rows doubled; red = picture top/bottom, yellow = the bottom of the band) and their machine read-backs (every frame)
| render | frames | top bar moved | bottom bar moved (= the top's moves; bottom = top + 239 clipped) | decisive picture shifts in the stabilized output |
|---|---|---|---|---|
| engine_SP_stab.mp4 | 608 | 0 / 0 | 69 / 8 | 7 / 6 (the content-motion class: Codex's comb at those units stays at 0) |
| engine_off_stab.mp4 | 607 | 1 / 0 | 11 / 98 | 9 / 67 (the raw pass: torn tops and its genuine one-line inter-field misregistration) |
| engine_EP_stab.mp4 | 621 | 0 / 2 | 228 / 8 | 3 / 5 (the crop follows the EP recording's own +2↔+3 jitter; the picture stays) |
| engine_composite_stab.mp4, stable interval (frames ≥ 338) | 582 | 0 / 6 | 10 / 50 | 4 / 42 (field 2: the 286↔287 chatter of the lone sub-black class, e.g. frames 424–501) |
| engine_SP_vstab_on_vs_off_4panel.mp4 | 607 | 1670×972 | — | — |

## 3d. Codex turn 12 (c4beaaa; 55 tests): score of run E (8d0fdeb) — not accepted on any capture
- Reference extension: `first_full_other_head_line` (internal blanking run, persistent three-third step, or decisive two-sided whole-row lag; −1 where not exposed) separate from `switch_first_line` (the earliest unreliable row). The commercial rows 6645 field 1 and 6672 field 2 now read 23 and 286 in the reference (the texture gate stands down at middle-row correlation 0.2; a dark row with recorded chroma noise is picture) — the same two premises the engine adopted, and the engine still reads 24/287 there through its sub-black rule (§5 item 1).
- Top histograms as in §2h (the two instruments agree on every count). Exact S against the new column, matches/mismatches: SP field 1 8/4 (of 12 exposed; 594 not exposed), field 2 95/60; commercial 463/49 and 501/6; EP 1/0 and 34/24; raw pass 195/370 and 428/156. Comb contradictions: SP 1/591, commercial 58/328, EP 13/329, raw pass 131/571.
- Commercial stable interval: reference first-full row 260 ×50 / 261 ×462 (four changes at 6645, 6688, 6714, 6738); engine S 260 ×136 / 261 ×443 with 57 changes — the engine's S is one row EARLY in ~86 units: the partial line carries part of the other head's horizontal blanking (Claude's census: 192 of 531 switch rows), so the blanking test fires on it and S names the partial row as 'entirely the other head'. EP former 178 exceptions → 17 (counters 2306–2358). No objections.

## 2c. Process notes (for the owner's judgement of the loop, not of the signal)
- Two false alarms cost ~50 minutes: (1) twelve engine processes each holding a whole capture pushed the host into 5.5 GB of swap (its own applications hold ~12 of 17 GB); the runs crawled and their output files froze — read as a hang until CPU time, page faults and a watchdog traceback showed a healthy loop being starved. Fixed in the tool (streaming, 0fc3461). (2) Python 3.14 buffers 128 KiB before the first file write, so a healthy run's CSV stays at 0 bytes for ~600 rows (15 minutes); read as a stall a second time. Fixed in the tool (flush per unit, fb390d9). Both are in memory notes.
- Runs killed and restarted: A and B (superseded code), C once. No result was taken from a partial file after the first mistaken reading of run B was noticed and discarded.
- Codex turn 10 was dispatched while the engine ran (the two instruments never idle at once); one dispatch, read in full, before the next brief.

## 2d. Run C (engine 0fc3461), commercial tape, against the rebuilt reference (2390a2f), joined by counter
- top: agrees in 590 (f1) / 570 (f2) units; engine one row LOWER (later) than the reference in 8+3 (f1) / 31+6+2 (f2); unmeasurable on either side 318/310 (the pre-551 rewind passage).
- stable interval (counter ≥ 6593, 582 units): measurable 569/582 (f1; run at 632b110: 458) and 570/582 (f2). Top 23 ×517, 25 ×51, 24 ×1 (f1); 286 ×516, 287 ×47, 288 ×5, 289 ×2 (f2). S 261 ×400 / 260 ×150 / 259 ×14 / ≤258 ×5 (f1); 523 ×510 / 522 ×34 / 524 ×18 / ≤521 ×7 (f2). Bottom (top+239 clipped) 262 ×569, 525 ×570, never changes. Violations of the stable test: 131 (top ≠ mode 52+54, |S − mode| > 1 18+7). The top violations sit in two passages (counters 6641–6674 and 6802–6819) where the first recorded line is dark (counter 6645: line 23 at 5.3/3.2, 24 at 15.3/8.9, 25 at 22.7/2.4 and a flat grey picture below) — both instruments read 24/25 there (the reference 'inferred') and 23 where the first line is grey (counter 6842: line 23 at 19.7) or where lines 23–27 are all dark (counter 6672). A per-unit reading cannot tell a dark first picture line from the tape's black line 22 before a brighter picture; the picture's constancy across the tape (line 23 carries grey picture at 6842) says it is picture. Listed as a residual for the owner (§6); the engine's rule (g) drops the level-based dark/bright distinction so the top no longer depends on the pedestal.
- S over-extension (the ≤258 / ≤521 units): two mechanisms read on the rows — (1) counter 6657, a flat grey field: the whole-row-lag test and the leading-run test fire on flat noisy rows (alignment is meaningless without texture; a picture-level 'run' ended by noise); (2) counter 6907: the row-two-above torn test judged against the one-above body envelope read seven picture rows as torn. Fixed as change (h): each test gated on its physical premise (textured rows; a run at black; the two-above pass against its own body maxima).
- S_tests census over the stable interval (which signature carried S): f1 RB/2RB 157, DB/2DB 124, B/2B 54, T/2T 35, …; f2 DB/2DB 114, RB/2RB 112, WDB/2DB 56, B/2B 34, F/2F 29 — the blanking-inside-the-row test (B) carries or co-carries S in the large majority; the dip-absent test (D) and the leading run (R) next.

## 4. Renders and read-backs
- engine, SP on pass, corrected top (`/private/tmp/hw-session/engine_SP_stab.mp4`, bottom = top+239 clipped, yellow = S): bottom bar moved 67 (f1) / 8 (f2) = exactly the top changes; no frame without a bar; decisive picture shifts left in the stabilized output: 5 (f1: 79, 102, 279, 295, 473) / 6 (f2: 75, 105, 280, 294, 451, 474).
  Classified by the per-field body shift (experiments/field_body_shift.py; 57 SP units where exactly one field moved decisively, 0 where both did): 279/280, 294/295, 473/474 alternate fields at consecutive units = content motion; 75, 76, 79, 102, 105 = one field moved one line while its top stayed (the owner's clamped-top case, §10.5). Body-corrected render (`engine_SP_bc_stab.mp4`, --body-correct, 11 corrections applied): residual shifts 5 (f1: 83, 106, 284, 308, 488) / 3 (f2: 88, 259, 468) — the carried offset moves the jump to where it is released instead of removing it; the rule is NOT adopted; the confirmation the owner named (comb between the fields) is Codex's turn 7.
- field-2 top: the engine reads 287 in units 87/258/439/467 where Codex's v3 has 286; the raw rows (zoom_SP_f2_top287.png) show line 286 at luma 3.8-6.0 std 3-4 (the tape's black line 22) and the picture from 287: the engine is right there, reported to Codex.
- Codex v3 references: rendered after turn 7 (comb)
- the on/off four-panel: `/private/tmp/hw-session/SP_vstab_on_vs_off_4panel.mp4` (aa8c238 references) and the raw re-paired `SP_raw_vstab_on_vs_off_4panel_repaired.mp4`

## 5. Residual list at run E (engine 8d0fdeb; every item with the rows it rests on; none tuned away)
1. **Commercial tape, a lone sub-black first line** (counters 6641–6674 and 6802–6819 in field 2, 115 units; field 1 counters 6642–6662, 24 units at 24 and 9 at 25): line 286 (or 23) at luma 3–5, flat, below the tape's pedestal 9–11, followed by the picture at 15–22. The per-unit rule reads the tape's black line 22 there and the top one row low; the tape's constancy (line 23 grey at counter 6842; the picture stable) says picture. Fixture A's displaced black line 22 looks identical per unit and the SP's 608/608 rests on that reading. Resolution proposed: a per-source constant (the black line 22 is always sub-black, flat, at one row, never grey) — the contract's own geometry lock; not a per-unit rule. The reference has the same passage as 'inferred' 24/25.
2. **Commercial tape, the half-row transition line** (counter 6645 field 1 and its neighbours, 9 units at 25): line 24 at 15.3/9.0 is a row whose left half is black and right half picture (the source's vertical blanking edge landing mid-line), uncorrelated with both neighbours; the engine reads 25, the reference 24 'inferred'.
3. **V-stabilize-off pass, flagged first lines** (recorded field 1 = the SP's field 2, 120 units at 288/289): with the line TBC off the first one or two lines carry a timing error that varies along the row, so no single lag (±24 samples) aligns them with the row below and the correlation test reads them as VBI. A per-segment alignment would be the fix; not built. The engine's 287 in 18 other units is right (Codex turn 11: line 286 at the noise floor).
4. **SP unit 333 (counter 13833) field 1 and unit 606 (14106) field 2**: the other head's row 260 carries a blank-level run of exactly 64 samples at luma 3.9 that the engine's blank tolerance (within 3.0 of 1.4) breaks into fragments, S 261 against the exposed 260 (within the partial ambiguity); unit 606: the departure begins at 522, engine S 520 (a two-above pass on diagonal content, unchanged since 632b110).
5. **EP units 31, 141, 438 (field 1) and 144, 195, 574 (field 2)**: S two rows from the reference's switch row; rows not looked at today.
6. **The stabilized SP output still moves in 7 (f1) / 6 (f2) frames**: units 79, 102, 75, 105 and their partners — content motion by Codex's comb (registration unchanged); the crop must not follow a body shift without a comb change. Unchanged since §4.
7. **The raw pass's genuine one-line inter-field misregistration** (56 units, Codex turn 8): in the signal, not the engine; visible as 67 decisive shifts in the off-pass render's field 2.
8. **S one row early where the partial line carries the other head's blanking** (commercial field 1 ~86 stable units, SP 3 of 12 exposed units, EP field 2 24): the mid-row blanking test fires on the partial (switch) row when the switch precedes the blanking interval within the row. Definitional refinement for the next iteration: a partial row's LEFT segment (before the run) still aligns with the row above at zero lag; a full other-head row's does not — S = the first row whose leading segment does not align. Not changed today (no rows looked at yet beyond the census).
9. **Commercial rewind passage (counters < 6593)**: not judged, by the owner's rule; both instruments read garbage there.
Closed today (with the rows): the commercial S over-extension (261/262 → 260/261; the mid-row blanking signature); the EP field-2 top (286 → 288); the EP field-1 top (the reference was one row late in 426 units, the engine's isolated-row class in 178, both fixed); the flat grey field (no-picture → measured); the raw pass's zero-lag exclusions; the S runs into the body on slanted content (EP counter 1967, commercial 6907); the pedestal from any flat row.

## 6. Open questions for the owner (output preferences only; the signal questions above are decided on rows)
1. A lone sub-black first line before the picture: on fixture A it is the tape's black line 22 (displaced field), on the commercial tape a crushed-black first picture line. Per unit the two are the same rows. Do you want the engine to carry a per-source constant for the black line 22 (the contract's lock: always sub-black, flat, at one row, never grey — learned over the first units, re-checked every unit), so the commercial's 115 units read 286 and the SP keeps 24? The per-unit instrument as it stands reads the fixture-A convention.
2. The V-stabilize-off pass's flagged first lines (120 units): leave them as measured (top one or two rows low with the picture torn there anyway), or have the engine align per segment so the top reads 286 under flagging? The pass is a fixture for what the line TBC does; the deliverable path has the TBC on.
3. The commercial rewind passage (before counter 6593): both instruments read garbage; the render shows it as measured. Blank it, or leave it?
