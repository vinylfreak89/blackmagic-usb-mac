# Geometry-first validation harness — brief to Codex (draft, awaiting the owner's go)

Role switch, per the owner: Claude writes the next registration engine; you write the validation harness. Neither of us reads the other's prior work: you do NOT read src/field_registration/ (any version), experiments/follow_audit.py, relative_comb_audit.py, gap_gauge_audit.py, v9_acceptance.py, line21_truth.py, raw_panel.py, or Claude's briefs about them; I do NOT read your engine code or its goldens. We guide each other on concept only. Branch: geometry-first (from main); you commit under experiments/harness/ (or a directory you name) and never under src/field_registration/.

The contract (owner's words, 2026-09-04/05): each field's active picture geometry in the Shuttle's pass-through region — top edge, bottom edge, height — measured every unit is the authority for where the crop goes; the tape's line 21, the black line 22, the static comb and any temporal witness only CONFIRM that reading where geometry alone cannot decide: (1) the head-switch region makes the bottom noisy and field-to-field inconsistent; (2) geometry does not say which field's lines interleave on top — field precedence is settled once per lock, by static comb, and held; (3) letterboxed or otherwise boxed pictures have their own geometry, centred in the raster, and black level is not assumed constant. Tracking breaks only when the picture tears VERTICALLY or the lock is lost — the owner puts both in one class: old geometry invalid, back to zero, re-acquire when the lock returns. Horizontal tearing is NOT a geometry event (the previous census keyed on it, wrongly). Dropout or RF noise that makes the bottom edge unstable is the other case: keep the previous decision, re-evaluate when it restabilises, adjust if needed. Line 22 never renders (RP-202): if the first picture-carrying row sits directly under the displaced line-21 position, it is line 22 and is discarded. The output picture never moves except at a segment's initial lock and after a damage hold clears.

Reference raster (measured 2026-09-05 on captures/deck_ext_input_nosource_30s.tpc, locked, no source; unit row r = NTSC line r+4): rows 0–6 padding Y16/C128 exactly; rows 7–15 blanking Y1.4; row 16 the Shuttle's timing pattern (line 20); row 17 the Shuttle's null CEA-608 insert (line 21); row 18 blanking (line 22); rows 19–260 pass-through (lines 23–264); rows 261–269 padding; rows 270–278 blanking; row 279 timing (line 283); row 280 insert (line 284); row 281 blanking (line 285); rows 282–522 pass-through (lines 286–526); rows 523–524 padding. Nothing on the tape above line 23 / 286 reaches us unless a displacement pushes it into the pass-through region.

What the harness must do — from the RAW 525-line raster and the engine's sidecar only, never from the engine's internals:
1. An independent geometry oracle per unit per field: top edge, bottom edge, height, with the head-switch band treated as noisy; the tape's line 21 (parity-decoded CEA-608 anywhere in the field) and the black line 22 as independent readings where present; a static-comb relative-registration measurement of the PUBLISHED crops; a same-parity temporal witness of the picture body. Each reported with its own validity, never fused into the verdict.
2. The acceptance: for every unit, does the published crop start at the picture's first RP-202 line (line 23 / 286 plus the displacement) as the oracle reads it, with line 22 excluded? Report agree / off-by-n / oracle-abstains, per field, whole tape, with the reason distribution of the disagreements, and the run structure (a wrong crop for 800 units is one defect, not 800).
3. Never-bounce: count output-picture moves that are not (a) a segment's initial lock, (b) a damage-hold clearing, (c) real content geometry changes (boxing) — every other move is a defect; list them with unit numbers.
4. Field precedence: is the published pair interleaving in the right order (static comb on static detail), and does it ever change within a lock?
5. Holds: measured on the raw 2026-09-06, the 34:14–34:43 passage of fixture A contains only HORIZONTAL damage (a migrating mistracking bar; the first 6–12 lines of field 1 torn sideways in units 62,303, 62,306, 62,336–62,338; head-switch aftermath at the top of both fields around 62,320–62,340) with the picture body continuous and steady throughout — no geometry event. The engine must keep placing from the body/bottom where the top is torn and must NOT move the crop there (round 10 bounced field 1 +1→+3 and field 2 +3→+2 at 62,336 and rendered a vertical tear that the tape does not have). The one genuine lost-lock event found so far is the recording splice at whole-tape ordinal 300 (10.01 s): field 1 is two recordings in one field (rows 0–~150 outgoing, ~170–262 incoming, torn band between), field 2 already the incoming one, the deck's OSD unmoved in both halves — the golden for the lost-lock class (nothing in unit 300 places; the incoming recording is acquired fresh from 301). A second is expected at the program transition near 24:20. The harness reports every hold anywhere with its cause; any output move inside 34:14–34:43 is a defect; a re-acquisition anywhere other than a splice or a true signal loss is a defect.
6. Runs on fixture A (captures/fulltape.cap6, 86,293 exact units) and on the other captures in captures/ (composite, no-input, virgin transition, no-source) where the expected answer is "no picture" or a fixed one; deterministic; fails closed (every exact unit accounted for); exits nonzero on any defect.
Deliver as a small set of scripts under your directory with one runner, README, and a golden report format we agree on before you run it against my engine. First deliverable: the ORACLE alone, validated on the raw against the owner's named sites (05:00, 01:26, 35:00, 37:01, minute 43, the torn passage), so we agree on what "the geometry" reads before either of us trusts a verdict.
