# Owner-approved exceptions to scripts/git-hooks/artifact_guard.py, one shell-style glob per
# line, matched against the whole repository path. Adding an entry is the owner's decision,
# and a change to this file must be committed on its own. Empty by default (2026-09-16).
